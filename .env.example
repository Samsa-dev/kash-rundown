# WebSocket server URL (set this in Vercel environment variables)
# For local dev, leave empty — defaults to ws://localhost:3001
# For production, set to your Railway URL: wss://your-app.up.railway.app
VITE_WS_URL=
